# Saturn-IB sample models (Phase 8)

Generated by `__tests__/saturn-ib-slices.test.ts` from `examples/saturn-ib/sliceModels.ts`.

| Model | Description |
|-------|-------------|
| `saturn-8.1-gravity-ballistics.json` | 1D radial free-fall: r_dot=v, v_dot=-mu/r^2 with integrator x(0) and divide |
| `saturn-8.2-6dof-vacuum-kinematics.json` | Vacuum quaternion kinematics: body2quaternion_rates + integrator x(0) + quat_to_dcm (no force/torque) |
| `saturn-6dof-varmass-quaternion-eom.json` | 6-DOF variable-mass EOM with unit-quaternion renormalization, principal inertia ∝ mass, point-mass gravity, body forces/moments |
| `saturn-6dof-vehicle-burn-demo.json` | Vehicle burn demo: EOM_6DoF_VarMass subsystem with axial thrust LUT + mdot after liftoff edge |
| `saturn-9.1-open-loop-6dof-ascent.json` | Open-loop 6-DoF ascent: EOM + liftoff/thrust/mdot + altitude/atmosphere/q̄ plots (no aero force). Sprint integration model. |
| `saturn-9.2-closed-loop-pitch-rate-damp.json` | Closed-loop pitch-rate damp: EOM + short boost + Q feedback through TF/limit → My (FCC-style). Sprint 9.2. |
| `saturn-9.3-open-loop-6dof-ascent-aero.json` | Open-loop 6-DoF + simple aero drag (F_aero=−q̄·CdA·v̂) into EOM. Compare to TN-AP-67-158 Table 5 (not Simulink). |
| `saturn-9.4-open-loop-chi-6dof-ascent.json` | Simplified χ elev program on TN-class 6-DoF plant. |
| `saturn-9.5-open-loop-chi-table2b-ascent.json` | TN Table 2B χ_c (elev=90+χ_c) + rate loop. |
| `saturn-9.6-chi-table2b-attitude-pd.json` | Table 2B elev + body-pitch PD (θ̂=∫Q). Primary residual candidate. |
| `as205-reference/as205_trajectory_reference.csv` | Digitized TN-AP-67-158 Table 5 S-IB trajectory (SI). |
| `as205-reference/as205_table2b_chi.csv` | Digitized Table 2B pitch command (χ_c and elev). |
| `saturn-8.3-engine-thrust-timer.json` | Edge detect on start command resets integrator; time-since-start drives thrust lookup_1d |
| `saturn-8.4-atmosphere-qbar.json` | COESA atmosphere + dynamic pressure q=0.5*rho*V^2 during climb |
| `saturn-8.5-stage-enable-freeze.json` | S-IB stage subsystem with enable: integrates propellant while enable=1, freezes after sep (t≥10s) |
| `saturn-8.7-rate-modulator.json` | Relay + unit_delay bang-bang modulator (SACS-style skeleton) |
| `saturn-8.8-chi-time-tilt.json` | Time-tilt chi pitch program via lookup_1d + rate_limiter |
| `saturn-8.9-igm-mode-shell.json` | IGM mode variable nIGMMode via data_store write/read (time-based mode steps) |
| `saturn-8.6-fcc-filter.json` | Attitude error through transfer_function + limit (FCC skeleton) |
| `saturn-8.10-open-loop-ascent-1d.json` | 1D open-loop ascent: liftoff edge → thrust table, gravity, atmosphere density (order-of-magnitude stack) |

## Blocks exercised

| Slice | New / key blocks |
|-------|------------------|
| 8.1 gravity | integrator x(0), divide, multiply, uminus |
| 8.3 engine timer | edge_detect, integrator reset, lookup_1d |
| 8.4 atmosphere q̄ | atmosphere (4 outs), multiply |
| 8.6 FCC | transfer_function, limit |
| 8.7 rate modulator | relay, unit_delay |
| 8.8 chi time-tilt | lookup_1d, rate_limiter, clock |
| 8.9 IGM mode | data_store_write/read, evaluate |

AS-205 constants from `saturn-1B/AS205_presettings.m` are included on relevant models as model parameters.
